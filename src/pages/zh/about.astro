---
// /zh/about — single-source-of-truth: the canonical /about page renders
// both locales based on the URL. Redirect any direct hit on /zh/about to
// the canonical page so we don't drift two copies.
return Astro.redirect('/about', 301);
---